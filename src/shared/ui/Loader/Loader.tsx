import { LoaderIcon } from "@/shared";
import SvgWrapper from "../SvgWrapper";

const Loader = () => {
  return (
    <SvgWrapper size="48">
      <LoaderIcon />
    </SvgWrapper>
  );
};

export default Loader;
